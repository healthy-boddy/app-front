import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonContainer: {
    width: "98%",
    height: "auto",
    alignItems: "center",
    backgroundColor: "#fff",
    borderRadius: 56,
  },
  buttonText: {
    textAlign: "center",
    color: "#4C64FF",
    padding: 12,
    marginLeft: 10,
    marginRight: 10,
    width: "auto",
  },
});
